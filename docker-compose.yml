version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend/siem.db:/app/siem.db

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend